const Change = require('../models/Change')

module.exports = {
    async createChange(req, res) {
        
    },

    async deleteChange(req, res) {
        
    },

    async editChange(req, res) {
        
    },

    async listChanges(req, res) {
        
    }
}